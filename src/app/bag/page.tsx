import type { Metadata } from 'next';
import { CartPageClient } from '@/components/cart/CartPageClient';
import { getCustomerSession } from '@/lib/customer-auth';
export const metadata:Metadata={title:'Your Bag',description:'Review the camera gear in your Shutterbug bag before checkout.',robots:{index:false,follow:false}};
export default async function BagPage(){const customer=await getCustomerSession();return <main className="bg-cream px-4 py-8 sm:px-6 sm:py-12 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-sm font-bold uppercase tracking-[.22em] text-moss">Shutterbug bag</p><h1 className="mt-2 font-serif text-4xl font-bold text-ink sm:text-6xl">Your Bag</h1><p className="mt-3 max-w-2xl leading-7 text-ink/65">Review your finds. We confirm current price and availability before secure payment.</p><CartPageClient signedIn={Boolean(customer)}/></div></main>}